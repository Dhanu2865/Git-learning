# Merge vs Rebase

### Merge
It combines the branches and keeps history of changes.
- git merge branch name

### Rebase
It rewrites the history. Cleaner version of history. It moves the branch to the latest base
- git rebase main

### When to use
- Merge → Default choice
- Rebase → Clean history
